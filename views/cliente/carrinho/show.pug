extends ../layout

block page_title
  | Carrinho de Compras

block main_content
  .content
    .container
      .row
        .col-md-12
          p.text-muted.lead Você possui #{carrinho.totalItens} ítem(s) em seu carrinho.
        #basket.col-md-9.clearfix
          .box
            form.form-carrinho(method='post', action='#')
              .table-responsive
                table.table
                  thead
                    tr
                      th(colspan='2') Produto
                      th Quantidade
                      th Preço unitário
                      th Desconto
                      th(colspan='2') Total
                  tbody
                    each item in carrinho.itens
                      tr
                        td
                          a(href='#')
                            img(src=item.produto.imagem)
                        td
                          a(href='/produtos/' + item.produto.id)= item.produto.nome
                        td
                          input.form-control(type='hidden', name="item[id]", value=item.id, id="inputItemId" + item.id)
                          input.form-control(type='number', name="item[quantidade]", value=item.quantidade, min="1", id="inputQuantidade" + item.id, required)

                        td R$ #{item.produto.preco}
                        td R$ 0.00
                        td R$ #{item.total}
                        td
                          a(href='/cliente/itens/' + item.id + '/delete')
                            i.fa.fa-trash-o
                  tfoot
                    tr
                      th(colspan='5') Total
                      th(colspan='2') R$ #{totalDaCompra}
              .box-footer
                .pull-left
                  a.btn.btn-default(href='/')
                    i.fa.fa-chevron-left
                    |  Continuar comprando
                .pull-right
                  button.btn.btn-default(type="submit" name='update_button' value='update_button')
                    i.fa.fa-refresh
                    |  Atualizar carrinho
                  button.btn.btn-template-main(type="submit" name='finish_button' value='finish_button')
                    | Finalizar compra
                    i.fa.fa-chevron-right
        .col-md-3
          #order-summary.box
            .box-header
              h3 Pedido
            p.text-muted
              | Shipping and additional costs are calculated based on the values you have entered.
            .table-responsive
              table.table
                tbody
                  tr
                    td Order subtotal
                    th $446.00
                  tr
                    td Shipping and handling
                    th $10.00
                  tr
                    td Tax
                    th $0.00
                  tr.total
                    td Total
                    th $456.00

  script.
    App.Cliente.Carrinho.show(!{JSON.stringify(carrinho, currentUser)});